{
  "name": "linkedin-bot",
  "version": "2.0.0",
  "description": "Multi-account LinkedIn automation server for n8n",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "install-browsers": "npx playwright install chromium && npx playwright install-deps chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0",
    "dotenv": "^16.4.5"
  }
}
